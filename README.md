"# expense-tracker" 

## Instructions

```bash
# 1, clone the project
cd project expense_tracker

#, 2, install dependancies
pip install -r requirements.txt

# 3, install the CLI globally

pip install -e .

```